% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_compare_saturation.R
\name{plot_compare_saturation}
\alias{plot_compare_saturation}
\title{Plot saturation comparison across threshold sets}
\usage{
plot_compare_saturation(compare_results, thresholds_list)
}
\arguments{
\item{compare_results}{A data frame with at least \code{code}, \code{count}, and one or
more logical columns indicating whether the code meets each threshold set.}

\item{thresholds_list}{A named list of thresholds used to generate
\code{compare_results}. The names will be used as facet labels.}
}
\value{
A ggplot object showing code saturation by threshold set.
}
\description{
This function plots the saturation of codes across multiple threshold sets.
Codes that do not meet the threshold in a given set are excluded (not plotted).
}
\examples{
library(dplyr)
library(ggplot2)

compare_results <- tibble::tibble(
  code = c("code_a", "code_b", "code_c"),
  count = c(10, 5, 2),
  Set1 = c(TRUE, TRUE, FALSE),
  Set2 = c(FALSE, TRUE, TRUE)
)

thresholds_list <- list(Set1 = 3, Set2 = 5)

plot_compare_saturation(compare_results, thresholds_list)
}
