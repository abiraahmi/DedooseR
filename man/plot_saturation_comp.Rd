% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_saturation_comp.R
\name{plot_saturation_comp}
\alias{plot_saturation_comp}
\title{Compare Code Saturation Across Multiple Threshold Sets}
\usage{
plot_saturation_comp(
  df_all_summary,
  df_qual_summary,
  thresholds_list,
  stacked = TRUE,
  as_proportion = FALSE,
  ncol = 2
)
}
\arguments{
\item{df_all_summary}{A data frame containing overall code summary
information, including at least
the \code{Code} column and \code{total_preferred_coder} counts.}

\item{df_qual_summary}{A data frame containing quality indicator counts for
each code. Columns
should include \code{Code} and columns with names ending in \verb{_Count}
representing counts per quality indicator.}

\item{thresholds_list}{A named list of named lists. Each inner named list
specifies minimum count
thresholds for each quality indicator. For example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(
  Set1 = list(Heterogeniety = 3, `Priority excerpt` = 2),
  Set2 = list(Heterogeniety = 1, `Priority excerpt` = 3)
)
}\if{html}{\out{</div>}}}

\item{stacked}{Logical; if TRUE, bars are stacked. If FALSE, bars are dodged
side-by-side.}

\item{as_proportion}{Logical; if TRUE, plots proportions relative to the
total counts per code,
otherwise plots raw counts.}

\item{ncol}{Number of columns to arrange the multiple plots into.}
}
\value{
A patchwork object containing combined ggplot2 bar charts, one for
each threshold set.
}
\description{
Creates stacked (or side-by-side) bar charts visualizing code counts by
quality indicators
for multiple sets of user-defined thresholds. Each set of thresholds
corresponds to a unique
plot, which are combined into a multi-panel figure.
}
\examples{
\dontrun{
thresholds <- list(
  Set1 = list(Heterogeniety = 3, `Priority excerpt` = 2),
  Set2 = list(Heterogeniety = 1, `Priority excerpt` = 3)
)
plot_saturation_comp(df_all_summary, df_qual_summary, thresholds,
stacked = TRUE, as_proportion = FALSE)
}

}
